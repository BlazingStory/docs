<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <OverrideHtmlAssetPlaceholders>true</OverrideHtmlAssetPlaceholders>

    <!-- ".vec" is not in the SDK's default list, so add it to get search-index.vec.br/.gz. -->
    <CompressionIncludePatterns>$(CompressionIncludePatterns);**/*.vec</CompressionIncludePatterns>

    <BlazorWasmPrerenderingOutputStyle>AppendHtmlExtension</BlazorWasmPrerenderingOutputStyle>
    <BlazorWasmPrerenderingDeleteLoadingContents>true</BlazorWasmPrerenderingDeleteLoadingContents>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BlazorWasmPreRendering.Build" Version="8.0.2" />
    <PackageReference Include="Markdig" Version="1.3.2" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" Version="10.0.11" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" Version="10.0.11" PrivateAssets="all" />
    <PackageReference Include="PublishSPAforGitHubPages.Build" Version="3.0.3" />
  </ItemGroup>

  <!-- Documentation content, served at "/Docs" from beside "wwwroot" instead of from inside it. -->
  <ItemGroup>
    <Content Remove="Docs\**" />
    <Content Include="Docs\**" TargetPath="wwwroot\Docs\%(RecursiveDir)%(Filename)%(Extension)"
             ContentRoot="$(MSBuildProjectDirectory)\Docs\" />
  </ItemGroup>

  <!--
    Everything below writes files that the Git repository does not have, which is why each of them
    hooks "Restore" as well as "Build". See "What the build generates" in AGENTS.md.
  -->

  <!-- transformers.js bundle, for the embedding model of the search box. -->
  <PropertyGroup>
    <TransformersBundleJs>wwwroot\lib\transformers\transformers.min.js</TransformersBundleJs>
    <TransformersBundleDts>ts\lib\transformers\transformers.min.d.ts</TransformersBundleDts>
  </PropertyGroup>

  <Import Project="..\jsmodules\transformers\TransformersBundle.targets" />

  <!-- Prism bundle, for the syntax highlighting of the code blocks. -->
  <PropertyGroup>
    <PrismBundleJs>wwwroot\lib\prism\prism.js</PrismBundleJs>
  </PropertyGroup>

  <Import Project="..\jsmodules\prism\PrismBundle.targets" />

  <!-- "wwwroot/js/*.js", compiled by "tsc" from "ts/". Needs both bundles above in place first. -->
  <ItemGroup>
    <TypeScriptInput Include="ts\**\*.ts;tsconfig.json;package.json;package-lock.json" />
    <TypeScriptModule Include="ts\js\**\*.ts" Exclude="ts\js\**\*.d.ts" />
  </ItemGroup>

  <Target Name="CompileTypeScript" BeforeTargets="Restore;Build"
          DependsOnTargets="BuildTransformersBundle;BuildPrismBundle"
          Inputs="@(TypeScriptInput)" Outputs="@(TypeScriptModule -> 'wwwroot\js\%(RecursiveDir)%(Filename).js')">
    <Exec Command="npm ci" WorkingDirectory="$(MSBuildProjectDirectory)"
          Condition="!Exists('$(MSBuildProjectDirectory)\node_modules')" />
    <Exec Command="npm run build" WorkingDirectory="$(MSBuildProjectDirectory)" />
  </Target>

  <!-- Vector search index of every documentation version, built by the Docs.IndexGenerator app. -->
  <ItemGroup>
    <SearchIndexInput Include="Docs/versions.json" />
    <SearchIndexInput Include="Docs/*/sidebar.json" />
    <SearchIndexInput Include="Docs/*/EN/**/*.md" />
  </ItemGroup>

  <Target Name="GenerateSearchIndex" BeforeTargets="Restore;Build" Inputs="@(SearchIndexInput)" Outputs="$(BaseIntermediateOutputPath)search-index.stamp">
    <Exec Command="dotnet run --project &quot;$(MSBuildThisFileDirectory)..\Docs.IndexGenerator\BlazingStory.Docs.IndexGenerator.csproj&quot; -- --docs &quot;$(MSBuildThisFileDirectory)Docs&quot;" />
    <MakeDir Directories="$(BaseIntermediateOutputPath)" />
    <Touch Files="$(BaseIntermediateOutputPath)search-index.stamp" AlwaysCreate="true" />
  </Target>

</Project>
